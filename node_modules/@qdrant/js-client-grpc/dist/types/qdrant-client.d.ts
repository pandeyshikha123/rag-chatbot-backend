import { GrpcClients } from './api-client.js';
export type QdrantClientParams = {
    port?: number | null;
    apiKey?: string;
    https?: boolean;
    prefix?: string;
    url?: string;
    host?: string;
    timeout?: number;
    checkCompatibility?: boolean;
};
export declare class QdrantClient {
    private _https;
    private _scheme;
    private _port;
    private _prefix;
    private _host;
    private _grcpClients;
    private _restUri;
    constructor({ url, host, apiKey, https, prefix, port, timeout, checkCompatibility, }?: QdrantClientParams);
    /**
     * API getter
     *
     * @param string Name of api
     * @returns An instance of a namespaced API, generated from grpc services.
     */
    api<T extends keyof GrpcClients>(name: T): GrpcClients[T];
}
